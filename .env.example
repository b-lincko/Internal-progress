# CMMC Tracker - Environment Variables
# Copy this to .env and change the values

# PostgreSQL Password (change this!)
DB_PASSWORD=changeme-strong-password-here

# JWT Secret for auth tokens (must be at least 32 chars)
JWT_SECRET=changeme-jwt-secret-key-min-32-characters-long
